name: Kernel Builder

on:
  workflow_dispatch:
  push:
    branches:
      - lineage-22.2

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout kernel source
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Setup build environment
      run: |
        sudo apt-get update
        sudo apt-get install -y git flex bison ccache gcc g++ make libncurses5-dev curl unzip

    - name: Clone Proton Clang toolchain
      run: |
        git clone https://github.com/kdrag0n/proton-clang.git --depth=1 ~/proton-clang

    - name: Setup kernel variables
      run: |
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "SUBARCH=arm64" >> $GITHUB_ENV

    - name: Apply KernelSU Next
      run: |
        curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" | bash -

    - name: Build kernel
      run: |
        make -j$(nproc) \
          ARCH=arm64 \
          olddefconfig \
          CC=~/proton-clang/bin/clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=~/proton-clang/bin/aarch64-linux-gnu- \
          HOSTCC=gcc \
          HOSTCXX=g++ \
          Image.gz dtbs

    - name: Create Image.gz-dtb
      run: |
        cat arch/arm64/boot/Image arch/arm64/boot/dts/vendor/qcom/*.dtb > Image.gz-dtb
        gzip -f Image.gz-dtb

    - name: Build dtbo.img
      run: |
        mkdir -p dtbo_tmp
        cp arch/arm64/boot/dts/vendor/qcom/*.dtbo dtbo_tmp/
        dtboimg create --output dtbo.img dtbo_tmp/*.dtbo

    - name: Upload compiled kernel
      uses: actions/upload-artifact@v4
      with:
        name: umi-kernel-artifacts
        path: |
          Image.gz-dtb
          dtbo.img
          .config
